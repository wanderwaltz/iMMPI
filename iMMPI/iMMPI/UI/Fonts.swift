import UIKit

enum Fonts {
    static let solidButtonTitle = UIFont.preferredFont(forTextStyle: .body)

    static let scaleGroupTitle = UIFont.preferredFont(forTextStyle: .title3)
    static let majorScaleTitle = UIFont.preferredFont(forTextStyle: .body)
    static let minorScaleTitle = UIFont.preferredFont(forTextStyle: .subheadline)

    static let analysisDateHeaderTitle = UIFont.preferredFont(forTextStyle: .caption1)
}
